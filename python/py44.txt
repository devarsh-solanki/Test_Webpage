# 2.(1) exersice
# def reverse_list(l):
#     l.reverse()
#     return l

# 2.(2)

# def reverse_list(l):
#     return l[::-1]

# 2.(3)

def reverse_list(l):
    r_list = []
    for i in range(1,len(l)+1):
        popped_list = l.pop()
        r_list.append(popped_list)
    return r_list

numbers = list(range(1,11))
print(reverse_list(numbers))